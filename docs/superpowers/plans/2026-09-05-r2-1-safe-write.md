# R2.1 – bezpečný zápis Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Zapsat cílený patch s prokazatelnou ochranou ruční práce a obnovitelným auditem.
**Architecture:** Čistá příprava patche v core, persistentní journal v SQLite a Windows writer za portem. Žádné publikování bez úspěšné W1 brány.
**Tech Stack:** TypeScript, Zod, node:sqlite, Vitest, Windows integrační probe.
**Spec:** [R2 návrh](../specs/2026-09-05-r2-change-lifecycle-design.md), §4–5.
**Společné kontrakty:** [R2 index](2026-09-05-r2-implementation.md).

## Global Constraints

- Spec: `docs/superpowers/specs/2026-09-05-r2-change-lifecycle-design.md` (schváleno autorem v této relaci).
- Windows 11; současný repository runtime: Node `24.19.0`, npm `>=11 <12`, TypeScript `6.0.3`. Před instalací ověřit `node --version` a `npm.cmd --version`; neobcházet runtime gate.
- `RunAgentLoop` zůstává jedinou agentní smyčkou.
- R2 neslibuje izolaci libovolného shellu od souborů nebo sítě.
- Backend je zdroj pravdy; bez produktového UI, cloudu, vzdáleného Gitu a subagentních produktových funkcí.
- Migrace 0–3 a jejich checksumy se nemění. Produkční kód neimportuje `tests/support`.
- Jeden patch mění jeden existující textový soubor; UTF-8 s volitelným BOM, původní konce řádků, nejvýše 1 MiB před i po změně.
- Model ani CLI nesmí zadat vlastní runId, projectId nebo obnovený obsah pro privilegovanou operaci. Tyto identity doplní backend.
- Dokončovací důkaz vychází ze skutečných výsledků, ne z textu modelu. Fake a live brána jsou oddělené.
- Každý úkol končí testem, kontrolou diffu a samostatným commitem pouze jeho souborů. Push/PR vyžadují explicitní zadání.
- Nezasahovat do `Dokumentace příklady/`. Fixture a uživatelská data držet mimo skutečný uživatelův projekt.
- Všechny uvedené příkazy běží z kořene Codrynu. Na Windows používat `npm.cmd`; test runner má limit 15 s, host testy explicitně 30 s, celé gate procesy nejvýše 10 minut.
- Příkazy v tomto dokumentu jsou budoucí kroky implementace; při psaní plánu se nespouštěly.


## W1: Prokázat chráněné publikování na Windows

**Files – vytvořit:** `scripts/spikes/r2-write-probe.mjs`,
`scripts/spikes/r2-write-worker.ps1`,
`backend/infrastructure/test/windows-write-probe.test.ts`,
`docs/decisions/0005-r2-guarded-write.md`.
**Upravit:** `vitest.config.ts` (host-integration seznam).
**Consumes:** podporovaný Windows 11 a syntetická temp složka.
**Produces:** JSON `WriteProbeReport`; úspěšné ADR je vstup W4.

- [ ] **1. Přidat reportový kontrakt a červený test.** Probe musí selhat i tehdy,
  když lze stale patch závodem publikovat pouze jednou ze 100 pokusů.

```ts
// Lokální typ v testu; probe zapisuje tentýž tvar do stdout.
type WriteProbeReport = {
  supported: boolean;
  partialPublications: number;
  overwrittenExternalWrites: number;
  escapedPaths: number;
  cases: { name: string; passed: boolean }[];
};
test('write probe never overwrites a competing editor', async () => {
  const { execFile } = await import('node:child_process');
  const { promisify } = await import('node:util');
  const { stdout } = await promisify(execFile)(
    process.execPath, ['scripts/spikes/r2-write-probe.mjs', '--iterations', '100'],
    { timeout: 25_000, maxBuffer: 64 * 1024 }
  );
  const report = JSON.parse(stdout) as WriteProbeReport;
  expect(report.supported).toBe(true);
  expect(report.partialPublications).toBe(0);
  expect(report.overwrittenExternalWrites).toBe(0);
  expect(report.escapedPaths).toBe(0);
  expect(report.cases.length).toBeGreaterThanOrEqual(8);
  expect(report.cases.every(c => c.passed)).toBe(true);
}, 30_000);
```

- [ ] **2. Spustit** `npm.cmd test -- backend/infrastructure/test/windows-write-probe.test.ts`.
  Před implementací FAIL kvůli chybějícímu probe; poté skutečné konflikty
  nesmějí být přeznačeny na skip.
- [ ] **3. Implementovat experiment, nikoli produkční writer.** Koordinovat
  dva procesy pomocí explicitních bariér po načtení, po kontrole a před
  publikováním; náhodný sleep není důkaz race testu. Každý případ izolovat
  v `os.tmpdir()`, povolit mazání pouze vlastní absolutní složky.
  Zkoumat Windows sharing/handle mechanismus a publikování celého souboru.
  Kandidát musí zachovat identitu cíle i při výměně nadřazené složky.

```js
// Kostra skutečného výsledku: počítadla plní pozorování, nikoli konstanty.
const result = {
  supported: false, partialPublications: 0,
  overwrittenExternalWrites: 0, escapedPaths: 0, cases: []
};
// report.supported nastav až ze všech jednotlivých důkazů a doloženého mechanismu.
process.on('uncaughtException', () => process.exit(1));
```

  Povinné případy: existující otevřený writer; in-place write; editor používající
  temp+rename; změna stejné délky/mtime; junction swap; rename rodiče; hardlink;
  pád při publikování. Zvlášť sledovat procesně přerušenou operaci a výpadek
  napájení: druhý se nesmí tvrdit ověřený pouhým ukončením procesu.
  Bezpečný odmítnutý pokus není selhání testu; částečně publikovaný soubor je.
  Neukládat `supported=true` jen na základě statistik bez vysvětlení
  ochrany intervalu check/write.

  Zdroje ověřené 5. 9. 2026: [Microsoft – file opening/sharing](https://learn.microsoft.com/en-us/windows/win32/fileio/creating-and-opening-files).
  Používá se princip kontroly sdílení přístupu; žádný převzatý zdrojový kód.
  Návrhový závěr o dostatečnosti pro Codryn je až výsledek probe.
- [ ] **4. Zopakovat test a zapsat ADR:** mechanismus, podporované filesystemy,
  odmítané cesty, důkaz atomicity, race scénáře, balení a známé meze.
  Pokud žádný mechanismus nevyhoví, stav ADR je zamítnuto, W4/W5 se nespouštějí.
  Nezavádět náhradní `read → hash → writeFile`.
- [ ] **5. Commit** jen probe/test/config/ADR:
  `test(r2): prove guarded Windows publication`.

## W2: Čistá příprava cílené textové změny

**Files – vytvořit:** `shared/src/r2-change.ts`, `shared/test/r2-change.test.ts`,
`backend/core/src/changes/ports.ts`, `backend/core/src/changes/prepare-patch.ts`,
`backend/core/test/prepare-patch.test.ts`.
**Upravit:** `shared/src/index.ts`, `backend/core/src/index.ts`.
**Consumes:** `PatchInput` z indexu.
**Produces:** `preparePatch(text: string, edits: PatchInput['edits']): string`;
`patchInputSchema`; `ChangeActor`, `ChangeEntry`, `MutationResult`,
`WriteIntent`, `MutationJournal`, `BlobStore`, `GuardedWriter` dle indexu.
`FileHashReader` dle indexu je read-only kanonicky omezený port pro recovery
a aktuálnost diffu; adapter doplní W4.

- [ ] **1. Přidat testy přesné náhrady.**

```ts
import { preparePatch } from '../src/changes/prepare-patch.js';
test('applies one unambiguous edit preserving CRLF', () => {
  expect(preparePatch('a\r\nb\r\n', [{ oldText: 'b', newText: 'c' }]))
    .toBe('a\r\nc\r\n');
});
test('rejects an ambiguous or missing source', () => {
  expect(() => preparePatch('aa', [{ oldText: 'a', newText: 'b' }]))
    .toThrow('R2_PATCH_AMBIGUOUS');
  expect(() => preparePatch('a', [{ oldText: 'z', newText: 'b' }]))
    .toThrow('R2_PATCH_SOURCE_MISSING');
});
```

- [ ] **2. Spustit** `npm.cmd test -- backend/core/test/prepare-patch.test.ts shared/test/r2-change.test.ts`.
  FAIL před implementací.
- [ ] **3. Implementovat deterministické hledání v původním textu.**
  Všechny rozsahy se hledají ve stejném vstupu, překryv je chyba; nahrazovat
  od posledního rozsahu, aby dřívější offsety zůstaly platné.

```ts
export function preparePatch(
  text: string, edits: readonly { oldText: string; newText: string }[]
): string {
  const ranges = edits.map(edit => {
    if (!edit.oldText) throw new Error('R2_PATCH_SOURCE_MISSING');
    const start = text.indexOf(edit.oldText);
    if (start < 0) throw new Error('R2_PATCH_SOURCE_MISSING');
    if (text.indexOf(edit.oldText, start + 1) >= 0)
      throw new Error('R2_PATCH_AMBIGUOUS');
    return { start, end: start + edit.oldText.length, replacement: edit.newText };
  }).sort((a, b) => a.start - b.start);
  for (let i = 1; i < ranges.length; i++) {
    if (ranges[i]!.start < ranges[i - 1]!.end)
      throw new Error('R2_PATCH_OVERLAP');
  }
  let output = text;
  for (const range of ranges.reverse())
    output = output.slice(0, range.start) + range.replacement + output.slice(range.end);
  if (output === text) throw new Error('R2_PATCH_NO_CHANGE');
  return output;
}
```

  Zod: path max 1024 znaků, hash přesně 64 malých hex znaků, 1–16 editací,
  součet UTF-8 bajtů old/new nejvýše 64 KiB. Odmítnout NUL, prázdné oldText
  a neznámé klíče; citlivé/kanonické cesty řeší writer, nikoli tento parser.
  Utf-8 byte limity počítat jednotným helperem `TextEncoder`.
  `ChangeEntry` neobsahuje text ani absolutní cestu.
- [ ] **4. Doplnit matrix:** překryv, opakující se text, 16/17 editací,
  64 KiB/64 KiB+1, BOM, LF/CRLF, prázdný výsledek, no-op, surrogate znaky.
  Stejný příkaz musí PASS; hranice kódování bajtů testuje W4.
- [ ] **5. Commit:** `feat(r2): define guarded patch contracts`.

## W3: Journal, obnovovací obsah a monotónní revize

**Files – vytvořit:** `backend/infrastructure/src/persistence/r2-change-migration.ts`,
`backend/infrastructure/src/persistence/sqlite-mutation-journal.ts`,
`backend/infrastructure/src/persistence/sqlite-workspace-store.ts`,
`backend/infrastructure/src/persistence/sqlite-change-set-store.ts`,
`backend/core/src/changes/change-set-store.ts`,
`backend/infrastructure/src/filesystem/content-blob-store.ts`,
`backend/core/src/workspace/ports.ts`,
`backend/infrastructure/test/r2-persistence.test.ts`.
**Upravit:** `backend/infrastructure/src/persistence/migrations.ts`,
`backend/infrastructure/src/persistence/open-database.ts`,
`backend/infrastructure/src/persistence/run-migrations.ts`,
`backend/infrastructure/src/index.ts`, `backend/core/src/index.ts`.
**Consumes:** W2 journal/blob kontrakty, existující event envelope.
**Produces:** `SqliteMutationJournal`, `SqliteWorkspaceStore`,
`ContentBlobStore`; migration 4. Konstruktory přijímají existující
`DatabaseSync`, `Clock` a `IdGenerator`; blob store přijímá absolutní
userData kořen mimo workspace.

- [ ] **1. Přidat migrační a transakční test** v izolované SQLite databázi.
  Testový helper `createChangeStoreFixture(): Promise<{journal:
  MutationJournal; intent: WriteIntent; revision(): number; eventCount():
  number; close(): Promise<void>}>` vytvořit v tomtéž testu. Naplní
  session/run/call přes existující R1 stores; ID nesmí obejít FK.

```ts
test('confirm is idempotent for audit and revision', async () => {
  const f = await createChangeStoreFixture();
  try {
    await f.journal.prepare(f.intent);
    const beforeEvents = f.eventCount();
    const first = await f.journal.confirm(f.intent.operationId);
    const second = await f.journal.confirm(f.intent.operationId);
    expect(second).toBe(first);
    expect(f.revision()).toBe(first);
    expect(f.eventCount()).toBe(beforeEvents + 1);
  } finally { await f.close(); }
});
```

- [ ] **2. Spustit** `npm.cmd test -- backend/infrastructure/test/r2-persistence.test.ts`.
  FAIL kvůli chybějícímu adapteru.
- [ ] **3. Přidat migraci 4 a implementovat journal.** Schéma:

```sql
CREATE TABLE workspaces (
  id TEXT PRIMARY KEY, root_identity TEXT NOT NULL UNIQUE,
  revision INTEGER NOT NULL CHECK(revision >= 0),
  fingerprint TEXT NOT NULL, git_identity TEXT,
  observation_complete INTEGER NOT NULL CHECK(observation_complete IN (0,1))
) STRICT;
CREATE TABLE change_sets (
  id TEXT PRIMARY KEY, project_id TEXT NOT NULL REFERENCES workspaces(id),
  run_id TEXT NOT NULL UNIQUE REFERENCES agent_runs(run_id),
  state TEXT NOT NULL CHECK(state IN ('open','sealed','reverting','reverted','conflicted','recovery_required')),
  base_revision INTEGER NOT NULL, next_sequence INTEGER NOT NULL DEFAULT 1,
  created_at TEXT NOT NULL,
  UNIQUE(id,project_id,run_id)
) STRICT;
CREATE TABLE change_entries (
  id TEXT PRIMARY KEY, set_id TEXT NOT NULL REFERENCES change_sets(id),
  project_id TEXT NOT NULL, run_id TEXT NOT NULL,
  call_id TEXT NOT NULL REFERENCES tool_calls(call_id),
  sequence INTEGER NOT NULL, path TEXT NOT NULL,
  before_hash TEXT NOT NULL, after_hash TEXT NOT NULL,
  before_blob TEXT NOT NULL, after_blob TEXT NOT NULL,
  kind TEXT NOT NULL CHECK(kind IN ('patch','revert')),
  reverses_id TEXT REFERENCES change_entries(id),
  UNIQUE(set_id,sequence),
  FOREIGN KEY(set_id,project_id,run_id) REFERENCES change_sets(id,project_id,run_id),
  FOREIGN KEY(run_id,call_id) REFERENCES tool_calls(run_id,call_id)
) STRICT;
CREATE TABLE mutation_intents (
  operation_id TEXT PRIMARY KEY,
  entry_id TEXT NOT NULL UNIQUE REFERENCES change_entries(id),
  state TEXT NOT NULL CHECK(state IN ('prepared','applied','not_applied','conflicted')),
  confirmed_revision INTEGER, created_at TEXT NOT NULL
) STRICT;
```

  `prepare` vloží entry+intent+metadata event atomicky, duplicitní ID přijme
  pouze při shodném významovém vstupu. `confirm` v BEGIN IMMEDIATE ověří
  prepared, zvýší revizi, uloží confirmed_revision, event a stav applied;
  opakované volání vrátí uloženou revizi. Jiné stavy odmítne.
  `entries` vrací jen applied operace v pořadí, nikdy prepared.
  `resolve` nepublikuje obsah; zaznamená not_applied/conflicted.
  Kořen se identifikuje kanonicky v infrastructure, ne textovou variantou cesty.

  `ChangeSetStore.open(projectId: string, runId: string): Promise<string>`
  založí právě jeden set běhu s base_revision a created_at.
  `reserveSequence(setId: string): Promise<number>` atomicky zvýší
  next_sequence a vrátí přidělené pořadí. Mezery po odmítnutém zápisu jsou
  přípustné. M2 otevře set po vytvoření agent runu, před první mutací,
  a předá jeho ID do run-bound ApplyPatch. W4 nextSequence je vazba
  na tuto metodu; žádný process-local čítač nad sdílenou DB.

  Blob: název SHA-256, ověření hashe při čtení, zapisovat do unikátního temp
  souboru, flush, publikování a ověření před `prepare`. Žádné následování
  odkazu z modelu. Retence jen nepřipnutých blobů: default 30 dní/256 MiB;
  dosažení limitu s připnutými daty odmítne nový zápis, nemaže recovery data.
  Oba obsahy jsou připnuté při open/recovery; orphan blob lze uklidit
  až po průchodu všech referencí.

  Před migrací existující DB vytvořit konzistentní SQLite backup (nikoli
  kopii samotného .db bez WAL); selhání zálohy migraci zastaví. Staré checksumy
  zůstávají stejné. Transaction helper vrací původní chybu i při rollback chybě.
- [ ] **4. Doplnit testy:** rollback při event insert chybě, duplicitní ID s
  jiným obsahem, nesprávný run/call, reopen, poškozený blob, plný disk,
  retention pin, v3→v4 s R0/R1 eventy, foreign_key_check a integrity_check.
  Ověřit také v0→v4 a zálohu s necheckpointovaným WAL.
- [ ] **5. Commit:** `feat(r2): persist mutation intents and recovery content`.

## W4: Aplikační zápis a idempotentní recovery

**Files – vytvořit:** `backend/core/src/changes/apply-patch.ts`,
`backend/core/src/changes/recover-mutations.ts`,
`backend/core/test/apply-patch.test.ts`,
`backend/core/test/recover-mutations.test.ts`,
`backend/infrastructure/src/filesystem/windows-guarded-writer.ts`,
`backend/infrastructure/test/guarded-writer.test.ts`.
**Upravit:** exporty core/infrastructure, `vitest.config.ts`.
**Consumes:** W1 pass ADR, W2/W3.
**Produces:** `ApplyPatch.execute(input: PatchInput, actor: ChangeActor,
signal: AbortSignal): Promise<MutationResult>`;
`classifyRecovery(before: string, after: string, actual: string | null):
'not_applied' | 'applied' | 'conflicted'`;
`RecoverMutations.execute(projectId: string, signal: AbortSignal): Promise<void>`.

- [ ] **1. Přidat červenou recovery tabulku a fault injection test.**

```ts
import { classifyRecovery } from '../src/changes/recover-mutations.js';
test.each([
  ['a', 'b', 'a', 'not_applied'],
  ['a', 'b', 'b', 'applied'],
  ['a', 'b', 'c', 'conflicted'],
  ['a', 'b', null, 'conflicted']
] as const)('recovery %s %s %s', (before, after, actual, state) => {
  expect(classifyRecovery(before, after, actual)).toBe(state);
});
```

  `ApplyPatch` constructor: `{writer: GuardedWriter; blobs: BlobStore;
  journal: MutationJournal; ids: IdGenerator; setId: string;
  nextSequence: () => Promise<number>; hash: (b: Uint8Array) => string}`.
  Fake writer drží bajty a počítá publish; fake journal umí selhat při confirm.
  Test požaduje po confirm chybě `recovery_required`, jediný publish
  a po dvou recovery průchodech jediný applied event.
- [ ] **2. Spustit** `npm.cmd test -- backend/core/test/apply-patch.test.ts backend/core/test/recover-mutations.test.ts`.
- [ ] **3. Implementovat pořadí a port adapter.**

```ts
export function classifyRecovery(before: string, after: string, actual: string | null) {
  if (actual === before) return 'not_applied' as const;
  if (actual === after) return 'applied' as const;
  return 'conflicted' as const;
}
```

  Apply: validace → writer.open a hash pod guardem → striktní UTF-8 decode
  → preparePatch → velikost → oba blob put → journal.prepare → poslední
  abort kontrola → publish → confirm. Všechny návraty uzavřou guard ve finally.
  Před prepare chyba nemá účinek; po publish nebo jeho nejasném selhání
  znamená recovery. Zrušení po skutečném publikování nesmí vymazat jeho audit.
  Když selže close, výsledek nesmí skrýt nejisté vlastnictví.

  Writer realizuje výhradně W1 doložený mechanismus. Zachová BOM a bajty
  nezměněných částí; odmítne nevalidní UTF-8/NUL/binary, ADS, device/UNC
  varianty, reparse/hardlink cíle, které W1 neprokáže bezpečné. Prostý
  `writeFile` nesmí implementovat publish. Konkrétní native balicí soubory
  doplní W1 ADR do této sekce před kódováním adapteru; jde o řízený výstup
  experimentu, ne předem schválenou závislost.

  Recover constructor přijímá `{journal: MutationJournal; files: FileHashReader}`.
  Recover načte pending záměry, přes files.readHash kanonicky pozoruje hash
  a volá confirm/resolve. Reader vrací null jen pro neexistující soubor;
  permission nebo nestabilní čtení je chyba a ponechá pending pro recovery.
  Hashový reader implementovat ve windows-guarded-writer.ts jako samostatný
  read-only export bez možnosti publikování.
  Nikdy automaticky nepřepisuje unknown stav ani znovu nespouští patch.
- [ ] **4. Integračně ověřit:** stale bez blob/zápisu, chyby v každé hranici
  prepare/publish/confirm, SIGINT po publish, změna junction, velikost
  1 MiB/+1, BOM, pád samostatného procesu. Přidat guarded-writer test do
  sériových host testů. W1 testy musí stále projít s produkčním adapterem.
- [ ] **5. Commit:** `feat(r2): apply and recover guarded file edits`.

## W5: Připojit zápis do existujícího harnessu a zabezpečit audit

**Files – vytvořit:** `backend/core/src/tools/change-tool.ts`,
`backend/core/src/tools/safe-tool-audit.ts`,
`backend/core/test/change-tool.test.ts`,
`backend/core/test/safe-tool-audit.test.ts`.
**Upravit:** `backend/core/src/tools/tool-registry.ts`,
`backend/core/src/tools/tool-execution-harness.ts`,
`backend/core/src/tools/controlled-permission-policy.ts`,
`backend/core/src/agent/model.ts`,
`backend/core/src/agent/ports.ts`, příslušné core testy.
**Consumes:** ApplyPatch, validovaný run/project binding backendu.
**Produces:** `file.patch@1`, `ToolExecutionContext = ChangeActor`;
`safeToolAudit(toolId: string, input: unknown): JsonValue`.

- [ ] **1. Přidat test auditního úniku.**

```ts
import { safeToolAudit } from '../src/tools/safe-tool-audit.js';
test('raw source and unexpected secrets never enter the tool audit', () => {
  const input = { path: 'src/a.ts', oldText: 'SECRET_CANARY_42',
    newText: 'source', apiKey: 'SECRET_CANARY_42' };
  expect(JSON.stringify(safeToolAudit('file.patch', input)))
    .not.toContain('SECRET_CANARY_42');
  expect(safeToolAudit('unknown.tool', input)).toEqual({ inputRecorded: false });
});
```

- [ ] **2. Spustit** `npm.cmd test -- backend/core/test/change-tool.test.ts backend/core/test/safe-tool-audit.test.ts backend/core/test/tool-execution-harness.test.ts`.
- [ ] **3. Rozšířit handler** o třetí `context: ToolExecutionContext`.
  R1 handlers jej mohou ignorovat. `ToolRisk` přidá `write_project`;
  povolení platí jen pro projektově svázaný nástroj s W4 canonical guardem.
  Žádný modelový parametr nesmí měnit actor, setId ani kořen.

```ts
export function safeToolAudit(toolId: string, input: unknown) {
  if (toolId !== 'file.patch') return { inputRecorded: false };
  if (typeof input !== 'object' || input === null)
    return { inputRecorded: false };
  // Úplnou textovou cestu ani obsah neukládat z nevalidovaného vstupu.
  return { inputRecorded: false, operation: 'targeted_patch' };
}
```

  Rozšířit o R1 zachovaná bezpečná metadata po validaci a R2 counts/hash;
  originální argumenty ponechat jen v paměti běhu. `createWithInitialEvent`
  dostane bezpečnou projekci už pro unknown/invalid tool. Tool result pro model
  obsahuje relativní cestu, before/after hash, ID změny a revizi; žádný blob
  locator. Recovery je strukturovaný neúspěch a zablokuje další mutace běhu.
- [ ] **4. Doplnit test:** actor spoofing, chybný vstup před policy,
  denied před writer.open, single audit, náhrada textu po čtení, pořadí
  přechodů. Znovu spustit R1 harness a repeatability; nepřejmenovávat jeho
  read-only golden eventy bez doložené nutnosti.
- [ ] **5. Spustit společné gate z indexu a commit**
  `feat(r2): expose audited patch tool through existing harness`.

## Výstup R2.1

Zápisový tool je dostupný pouze s doloženým writerem a journalem. Výsledek
obsahuje změnu a recovery stav; zatím netvrdí úspěšné ověření projektu.
W1/W4 failure nelze obejít fake adapterem.
